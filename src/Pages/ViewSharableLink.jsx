import { useParams } from "react-router-dom"

const ViewSharableLink = () => {

  const {id} = useParams();
  // id from url 
  
  return (
    <div>
      view
    </div>
  )
}

export default ViewSharableLink
